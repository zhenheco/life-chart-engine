# life-chart-engine — direct dependencies.
# Pinned to versions verified on CPython 3.12.
#
# NOTE: CPython 3.12 remains locked for this change. The old Python Zi Wei
# native dependency constraint is gone, so this can be revisited after checking
# the remaining dependencies and deployment image.

# 精確 pin：golden fixtures 依賴 byte-exact 輸出，任何 astronomy-engine 升版都要重驗 fixtures
astronomy-engine==2.1.19
fastapi==0.139.0
# Security pin for FastAPI's Starlette runtime dependency.
starlette==1.3.1
uvicorn==0.39.0
httpx==0.28.1
sentry-sdk==2.48.0
